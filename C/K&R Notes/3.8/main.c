//3.8: Goto and Labels

#include <stdio.h>

int main(void)
{
	//lol the book describes "goto" statements as "infinitely-abusable"
	
	/* This is a code layout for when you might need a goto:

	for (...)
	{
		for (...)
		{
			...
			if (disaster)
				goto error;
		}
	}

	error:
		// clean up the mess //
	*/

	//Goto statements are useful when dealing with a deeply nested structure.

	return 0;
}
